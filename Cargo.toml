[package]
name = "kv-store"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.8"
prost = "0.11"
tokio = { version = "1", features = ["full"] }
clap = { version = "4.0.32", features = ["derive"] }

[build-dependencies]
tonic-build = "0.8"
